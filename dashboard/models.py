"""
dashboard/models.py

No database models are needed for process management — process handles
are kept in memory in process_manager.py.

This file exists so Django's app registry is happy and so future models
(e.g. process run history, audit log) can be added here.
"""
